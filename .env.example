# Configurações do Banco de Dados PostgreSQL
POSTGRES_DB=esus
POSTGRES_USER=postgres
POSTGRES_PASSWORD=esus

# Configurações do e-SUS PEC
# URL para download do instalador do e-SUS
# Obtenha a versão mais recente em: https://sisaps.saude.gov.br/esus/
URL_DOWNLOAD_ESUS=https://arquivos.esusab.ufsc.br/PEC/26e603822f8adcc4/5.3.28/eSUS-AB-PEC-5.3.28-Linux64.jar

# Modo de Instalação
# Define se a instalação será de Produção (false) ou Treinamento (true)
# Modo Treinamento: usado para fins de capacitação e testes, sem dados reais
# Modo Produção: usado para ambiente de produção com dados reais
# Padrão: false (Produção)
ESUS_TRAINING_MODE=false

# Configurações de Backup Automático
# Diretório no host onde os backups serão salvos (caminho absoluto ou relativo)
# Padrão: ./backups (diretório 'backups' no diretório do projeto)
BACKUP_DIR=./backups

# Número de dias de retenção dos backups (backups mais antigos serão removidos automaticamente)
# Padrão: 7 dias
BACKUP_RETENTION_DAYS=7

# Configurações de Backup S3 (Opcional)
# Para habilitar backups no S3, descomente e configure as variáveis abaixo
# Também é necessário descomentar as linhas de instalação do AWS CLI no database/Dockerfile

# Nome do bucket S3 onde os backups serão enviados
# S3_BUCKET=meu-bucket-backups

# Região AWS do bucket (padrão: us-east-1)
# AWS_DEFAULT_REGION=us-east-1

# Credenciais AWS (necessárias para acesso ao S3)
# AWS_ACCESS_KEY_ID=sua_access_key
# AWS_SECRET_ACCESS_KEY=sua_secret_key

# Endpoint customizado (opcional - para S3-compatible services como MinIO, Wasabi, etc)
# AWS_ENDPOINT_URL=https://s3.wasabisys.com
